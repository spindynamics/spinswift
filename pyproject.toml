[project]
name = "spinswift"
version = "0.1.0"
description = "Atomistic python modules for spinswift"
readme = "README.md"
requires-python = "==3.11.*"
dependencies = [
    "pymatgen>=2025.10.7",
]
[tool.uv]
python-preference = "system"
